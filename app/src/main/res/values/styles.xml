<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- List item header. Font family: ABeeZee, Line height: 14sp -->
    <style name="list_item_header">
        <item name="android:textSize">@dimen/headerFontSize</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <!-- List item header. Font family: ABeeZee, Line height: 11sp -->
    <style name="list_item_text">
        <item name="android:textSize">@dimen/textFontSize</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <!-- App header -->
    <style name="app_header">
        <item name="android:textSize">48sp</item>
        <item name="android:textColor">#F1F1F1</item>
    </style>

</resources>
